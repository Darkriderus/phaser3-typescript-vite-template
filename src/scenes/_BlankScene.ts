import Phaser from 'phaser'

export default class _SCENENAME_ extends Phaser.Scene {
    static readonly KEY = 'main-menu'

    constructor() {
        super(_SCENENAME_.KEY)
    }

    preload() {
    }

    create() {
    }
}
